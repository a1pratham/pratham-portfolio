# Public URL of the deployed site (no trailing slash), e.g. https://prathamukey.in
# Set this in the hosting build environment once the domain is live. It enables the
# canonical URL, og:url, absolute og:image, structured-data url, sitemap.xml and robots.txt.
# Leave unset for local development.
VITE_SITE_URL=
